syntax = "proto3";
package com.example.grpc;

message UpdateRequest {
    string file_uuid = 1;
    string status = 2;
}

message Response {
    string file_uuid = 1;
    map<string, string> data = 2;
}

service MainFileService {
    rpc updateFileStatus(UpdateRequest) return (Response);
}
